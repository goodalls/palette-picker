language: node_js
node_js:
  - node
services:
  - postgresql
before_script:
  - psql -c 'CREATE DATABASE travis_ci_test;' -U postgres
test:
  adapter: postgresql
  database: travis_ci_test
deploy:
  provider: postgresql
  migrate: "knex migrate:latest"